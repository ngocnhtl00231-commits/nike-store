# 🎪 Biểu Đồ Luồng Hoạt Động

## 📊 Sơ Đồ Tìm Kiếm & Lọc

```
┌─────────────────────────────────────────────────────────────────┐
│                        TRANG WEB NIKE STORE                      │
└─────────────────────────────────────────────────────────────────┘
                              ↓
                    ┌─────────────────────┐
                    │   Tìm Kiếm & Lọc    │
                    └─────────────────────┘
                              ↓
        ┌─────────────────────────────────────────────────┐
        │                                                 │
   [Search]                                        [Gender Select]
   "Tìm tên"                                        Nam/Nữ/Unisex
        │                                                 │
        └────────────────┬────────────────────────────────┘
                         ↓
                [Price Select]
            Giá: Thấp→Cao / Cao→Thấp
                         ↓
                  [Reset Button]
                  Xóa tất cả bộ lọc
```

## 🔄 Luồng Xử Lý Tìm Kiếm

```
User gõ "Air" vào search
          ↓
  [Input Event Triggered]
          ↓
  currentFilters.search = "Air"
          ↓
  getFilteredProducts() chạy
          ↓
  ┌──────────────────────────────────┐
  │ Lọc sản phẩm:                    │
  │ - Tìm kiếm: name.includes("air") │
  │ - Lọc gender: (nếu có)           │
  │ - Sắp xếp giá: (nếu có)          │
  └──────────────────────────────────┘
          ↓
  renderProducts() chạy
          ↓
  ┌──────────────────────────────────┐
  │ Render lại grid:                 │
  │ - Xóa sản phẩm cũ                │
  │ - Tạo HTML cho sản phẩm mới      │
  │ - Insert vào DOM                 │
  └──────────────────────────────────┘
          ↓
  attachProductListeners() chạy
          ↓
  ┌──────────────────────────────────┐
  │ Gán event listeners:             │
  │ - Nút "Xem Chi Tiết" → openModal │
  │ - Nút "Thêm Giỏ" → addToCart    │
  └──────────────────────────────────┘
          ↓
  ⚡ KẾT QUẢ: Trang hiển thị chỉ "Nike Air Max 90"
             (KHÔNG RELOAD TRANG)
```

## 🎯 Luồng Lọc Theo Loại

```
User chọn "Nam" từ dropdown
          ↓
  [Change Event Triggered]
          ↓
  currentFilters.gender = "Nam"
          ↓
  getFilteredProducts() chạy
          ↓
  ┌─────────────────────────────────────┐
  │ Filter logic:                       │
  │ if gender === "Nam/Nữ" → INCLUDE  │
  │ if gender === "Unisex" → INCLUDE   │
  │ if gender === "Nam" → INCLUDE      │
  │ else → EXCLUDE                      │
  └─────────────────────────────────────┘
          ↓
  renderProducts() → attachProductListeners()
          ↓
  ⚡ KẾT QUẢ: Hiển thị 4 sản phẩm
     - Nike Air Max 90 (Nam)
     - Nike Blazer Mid (Nam)
     - Nike Revolution 7 (Nam/Nữ)
     - Nike Cortez Classic (Unisex)
     - Nike Dunk Low (Unisex)
     (KHÔNG RELOAD TRANG)
```

## 💰 Luồng Sắp Xếp Giá

```
User chọn "Giá: Thấp → Cao"
          ↓
  [Change Event Triggered]
          ↓
  currentFilters.sortPrice = "asc"
          ↓
  getFilteredProducts() chạy
          ↓
  ┌──────────────────────────────────┐
  │ Sort logic:                      │
  │ if "asc":                        │
  │   array.sort((a,b) =>           │
  │     a.price - b.price)          │
  │                                  │
  │ Result:                          │
  │ 1. 1,299,000 - Revolution 7      │
  │ 2. 1,699,000 - Court Legacy      │
  │ 3. 1,899,000 - Cortez Classic    │
  │ 4. 2,199,000 - Blazer Mid        │
  │ 5. 2,299,000 - Dunk Low          │
  │ 6. 2,399,000 - Air Max 90        │
  └──────────────────────────────────┘
          ↓
  renderProducts() → attachProductListeners()
          ↓
  ⚡ KẾT QUẢ: Hiển thị từ rẻ nhất đến đắt nhất
             (KHÔNG RELOAD TRANG)
```

## 🔄 Luồng Reset Bộ Lọc

```
User nhấn "Đặt lại"
          ↓
  [Click Event Triggered]
          ↓
  ┌──────────────────────────────────┐
  │ Reset everything:                │
  │ - currentFilters = {}            │
  │ - searchInput.value = ""         │
  │ - genderSelect.value = ""        │
  │ - priceSort.value = ""           │
  └──────────────────────────────────┘
          ↓
  renderProducts() (show all)
          ↓
  attachProductListeners()
          ↓
  ⚡ KẾT QUẢ: Hiển thị tất cả 6 sản phẩm
             (KHÔNG RELOAD TRANG)
```

## 🎨 Cấu Trúc HTML Động

### Trước (Hardcoded):
```html
<div class="products__grid">
  <article class="product">...</article>  <!-- Hardcoded -->
  <article class="product">...</article>  <!-- Hardcoded -->
  <article class="product">...</article>  <!-- Hardcoded -->
  ...
</div>
```

### Sau (Dynamic):
```html
<div class="products__grid">
  <!-- Được tạo bằng JavaScript khi page load -->
  <!-- Thay đổi khi user lọc/tìm kiếm -->
</div>

<script>
  // Page load
  → renderProducts()
  → innerHTML = HTML cho tất cả 6 sản phẩm
  
  // User tìm kiếm "Air"
  → renderProducts()
  → innerHTML = HTML chỉ cho 1 sản phẩm
  
  // User lọc "Nam"
  → renderProducts()
  → innerHTML = HTML cho 4 sản phẩm Nam-related
</script>
```

## 📱 Responsive Design Flow

```
┌─────────────────────────────────────┐
│         Page Load                   │
└─────────────────────────────────────┘
          ↓
  ┌───────────────────────────────┐
  │ Check Window Width            │
  └───────────────────────────────┘
          ↓
    ┌─────────────────────────────┐
    │  >= 1200px (Desktop)        │
    │ ┌─────────────────────────┐ │
    │ │ Filters Flex Row        │ │
    │ │ [S] [G] [P] [R]         │ │
    │ │ Products: 3 Columns     │ │
    │ └─────────────────────────┘ │
    └─────────────────────────────┘
             or
    ┌─────────────────────────────┐
    │  1024px-1199px (Tablet)     │
    │ ┌─────────────────────────┐ │
    │ │ Filters Flex Row        │ │
    │ │ [S] [G] [P] [R]         │ │
    │ │ Products: 2 Columns     │ │
    │ └─────────────────────────┘ │
    └─────────────────────────────┘
             or
    ┌─────────────────────────────┐
    │  768px-1023px (Mobile)      │
    │ ┌─────────────────────────┐ │
    │ │ Filters Flex Row (wrap) │ │
    │ │ [S] [G] [P] [R]         │ │
    │ │ Products: 2 Columns     │ │
    │ └─────────────────────────┘ │
    └─────────────────────────────┘
             or
    ┌─────────────────────────────┐
    │  < 480px (Small Mobile)     │
    │ ┌─────────────────────────┐ │
    │ │ Filters Column          │ │
    │ │ [Search]                │ │
    │ │ [Gender]                │ │
    │ │ [Price]                 │ │
    │ │ [Reset]                 │ │
    │ │ Products: 1 Column      │ │
    │ └─────────────────────────┘ │
    └─────────────────────────────┘
```

## 🎯 Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                      products Object                        │
│  {                                                          │
│    1: { name, gender, price, oldPrice, rating, ... }       │
│    2: { name, gender, price, oldPrice, rating, ... }       │
│    ...                                                      │
│  }                                                          │
└─────────────────────────────────────────────────────────────┘
                          ↓
            ┌─────────────────────────────┐
            │  currentFilters Object      │
            │  {                          │
            │    search: "",              │
            │    gender: "",              │
            │    sortPrice: ""            │
            │  }                          │
            └─────────────────────────────┘
                          ↓
        ┌───────────────────────────────────────┐
        │  getFilteredProducts()                │
        │  - Filter by search                   │
        │  - Filter by gender                   │
        │  - Sort by price                      │
        │  Returns: Filtered array              │
        └───────────────────────────────────────┘
                          ↓
        ┌───────────────────────────────────────┐
        │  renderProducts()                     │
        │  - Map products to HTML               │
        │  - Insert into .products__grid        │
        │  - Handle "no results" message        │
        └───────────────────────────────────────┘
                          ↓
        ┌───────────────────────────────────────┐
        │  attachProductListeners()             │
        │  - Add click handlers for buttons      │
        │  - Modal & Cart functionality         │
        └───────────────────────────────────────┘
                          ↓
                    ⚡ DOM Updated
                   (Hiển thị kết quả)
```

## 🔐 Event Listener Map

```
┌────────────────────────────────────────────┐
│          Event Listeners Setup             │
├────────────────────────────────────────────┤
│                                            │
│  #filterSearch                             │
│  ├─ Event: input                          │
│  └─ Action: Update search, renderProducts  │
│                                            │
│  #filterGender                             │
│  ├─ Event: change                         │
│  └─ Action: Update gender, renderProducts  │
│                                            │
│  #filterPrice                              │
│  ├─ Event: change                         │
│  └─ Action: Update price, renderProducts   │
│                                            │
│  #filterReset                              │
│  ├─ Event: click                          │
│  └─ Action: Reset all, renderProducts      │
│                                            │
│  .btn--secondary (Dynamic)                 │
│  ├─ Event: click                          │
│  └─ Action: openModal()                   │
│                                            │
│  .btn--add-cart (Dynamic)                  │
│  ├─ Event: click                          │
│  └─ Action: cart.addItem() + feedback     │
│                                            │
└────────────────────────────────────────────┘
```

---

## ✨ Highlights

✅ **No Page Reload** - Tất cả xử lý client-side
✅ **Real-time Updates** - Cập nhật ngay khi nhập/chọn
✅ **Efficient Rendering** - Chỉ render những gì cần thiết
✅ **Responsive** - Tự động thích ứng với kích thước màn hình
✅ **User-Friendly** - Giao diện trực quan, dễ sử dụng
✅ **Smart Filtering** - Nam/Nữ + Unisex được bao gồm hợp lý
